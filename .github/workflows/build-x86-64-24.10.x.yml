name: build-x86-64-immortalwrt-24.10-pure-daed

on:
  workflow_dispatch:
    inputs:
      luci_version:
        description: "固件版本 (稳定版锁定 24.10.5)"
        required: false
        default: "24.10.5"
        type: choice
        options: [24.10.0, 24.10.1, 24.10.2, 24.10.3, 24.10.4, 24.10.5]
      custom_router_ip:
        description: "管理 IP"
        required: true
        default: "10.10.10.1"
      profile:
        description: "根分区大小 (MB) - 推荐 4096"
        required: true
        default: '4096'
      enable_pppoe:
        description: "是否开启自动拨号"
        required: true
        default: 'yes'
        type: choice
        options: ['yes', 'no']
      pppoe_account:
        description: "宽带账号"
        required: false
      pppoe_password:
        description: "宽带密码"
        required: false

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set Permissions
        run: chmod +x ${{ github.workspace }}/x86-64/build24.sh

      - name: Prepare Config
        run: |
          mkdir -p "${{ github.workspace }}/custom"
          echo "${{ inputs.custom_router_ip }}" > "${{ github.workspace }}/custom/custom_router_ip.txt"

      - name: Inject Core Dependencies & Clean Bloat
        run: |
          # 1. J4125 物理机网卡与密码学硬件加速
          echo "CONFIG_PACKAGE_kmod-igc=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_kmod-igb=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_kmod-r8169=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_kmod-crypto-aesni=y" >> ${{ github.workspace }}/x86-64/imm.config
          
          # 2. daed 1.24.0 核心依赖防漏补全 (新增 XDP 与依赖)
          echo "CONFIG_PACKAGE_kmod-bpf=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_kmod-ebpf=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_kmod-tun=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_kmod-macvlan=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_kmod-xdp-sockets=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_bpftool=y" >> ${{ github.workspace }}/x86-64/imm.config
          
          # 3. 基础网络工具包 (保障后续获取外部规则库不出错)
          echo "CONFIG_PACKAGE_curl=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_ca-bundle=y" >> ${{ github.workspace }}/x86-64/imm.config
          echo "CONFIG_PACKAGE_ca-certificates=y" >> ${{ github.workspace }}/x86-64/imm.config

          # 4. 强制编译入 daed 核心与 UI (自动匹配 ImmortalWrt 对应内核版本)
          echo 'CUSTOM_PACKAGES="$CUSTOM_PACKAGES luci-theme-argon luci-app-argon-config luci-app-ttyd daed luci-app-daed \
          -luci-app-passwall -luci-app-homeproxy -luci-app-openclash -luci-app-ssr-plus -luci-app-vssr \
          -luci-app-samba4 -luci-app-ksmbd -luci-app-vsftpd -luci-app-nfs-utils -luci-app-cifs-mount \
          -luci-app-unblockneteasemusic -luci-app-xlnetacc -luci-app-zerotier -luci-app-upnp \
          -coreutils -mwan3"' >> ${{ github.workspace }}/shell/custom-packages.sh

      - name: Build Firmware
        run: |
          docker run --rm -i \
            --user root \
            -v "${{ github.workspace }}/bin:/home/build/immortalwrt/bin" \
            -v "${{ github.workspace }}/files:/home/build/immortalwrt/files" \
            -v "${{ github.workspace }}/custom:/home/build/immortalwrt/files/etc/config" \
            -v "${{ github.workspace }}/x86-64/imm.config:/home/build/immortalwrt/.config" \
            -v "${{ github.workspace }}/shell:/home/build/immortalwrt/shell" \
            -v "${{ github.workspace }}/x86-64/build24.sh:/home/build/immortalwrt/build.sh" \
            -e PROFILE=${{ inputs.profile }} \
            -e INCLUDE_DOCKER=no \
            -e ENABLE_PPPOE=${{ inputs.enable_pppoe }} \
            -e PPPOE_ACCOUNT=${{ inputs.pppoe_account }} \
            -e PPPOE_PASSWORD=${{ inputs.pppoe_password }} \
            immortalwrt/imagebuilder:x86-64-openwrt-${{ inputs.luci_version }} /bin/bash /home/build/immortalwrt/build.sh

      - name: Upload Artifacts
        uses: softprops/action-gh-release@v2.2.1
        with:
          tag_name: Final-Pure-J4125
          name: "J4125 物理机终极纯净版 (内核级适配 daed 1.24.0)"
          body: "已剔除所有干扰项。内嵌 kmod-xdp-sockets 及完整 eBPF 栈，确保物理机直通网络极低延迟。"
          files: ${{ github.workspace }}/bin/targets/x86/64/*squashfs-combined-efi.img.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
